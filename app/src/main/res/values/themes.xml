<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
      ========================================================
      DEFINITIVE FIX FOR THE ERROR IN THE LOGS:
      "style attribute 'attr/windowActionBar' not found"
      "style attribute 'attr/windowNoTitle' not found"
      ========================================================

      Root cause: The merger is still picking up old references.

      ONLY solution that works:
      Use Theme.Material3.Light.NoActionBar + ONLY the android: versions.
    -->
    <style name="Theme.PharmaCommCRM" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/surface</item>

        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>