<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
      ========================================================
      DEFINITIVE FIX FOR THE EXACT ERROR IN YOUR LOGS:
      "style attribute 'attr/windowActionBar' not found"
      "style attribute 'attr/windowNoTitle' not found"
      ========================================================

      Root cause: Resource merger is still seeing references to AppCompat
      attributes from old theme parents.

      ONLY reliable solution for AGP 8.x + Material3 + Compose:
      - Use Theme.Material3.Light.NoActionBar as parent
      - Explicitly provide BOTH the android: and non-android versions 
        of the two attributes.
    -->
    <style name="Theme.PharmaCommCRM" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/surface</item>

        <!-- These two lines are the KEY to fixing the error -->
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>